diff --git a/crypto/sha/sha512.c b/crypto/sha/sha512.c
index 4c3a3b6..f1a44f5 100644
--- a/crypto/sha/sha512.c
+++ b/crypto/sha/sha512.c
@@ -583,7 +583,7 @@ static SHA_LONG64 __fastcall __pull64be(const void *x) {
 static void sha512_block_data_order(SHA512_CTX *ctx, const void *in,
     size_t num)
 {
-    const SHA_LONG64 *W = in;
+    const SHA_LONG64 *W = (const SHA_LONG64 *)in;
     SHA_LONG64 A, E, T;
     SHA_LONG64 X[9 + 80], *F;
     int i;
@@ -645,7 +645,7 @@ static void sha512_block_data_order(SHA512_CTX *ctx, const void *in,
 static void sha512_block_data_order(SHA512_CTX *ctx, const void *in,
     size_t num)
 {
-    const SHA_LONG64 *W = in;
+    const SHA_LONG64 *W = (const SHA_LONG64 *)in;
     SHA_LONG64 a, b, c, d, e, f, g, h, s0, s1, T1, T2;
     SHA_LONG64 X[16];
     int i;
@@ -737,7 +737,7 @@ static void sha512_block_data_order(SHA512_CTX *ctx, const void *in,
     size_t num)
 #endif
 {
-    const SHA_LONG64 *W = in;
+    const SHA_LONG64 *W = (const SHA_LONG64 *)in;
     SHA_LONG64 a, b, c, d, e, f, g, h, s0, s1, T1;
     SHA_LONG64 X[16];
     int i;
